## Dependencies
You will need Anaconda or Minicoda installed. To create and activate the environment, run the following commands in a terminal:
```shell
conda env create -f environment.yml
conda activate fenics_env
```

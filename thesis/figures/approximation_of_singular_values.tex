\begin{figure*}[thbp]
    \centerline{\includegraphics[width=1.0\textwidth]{approximation_of_singular_values.png}}
    \caption{Ratio of approximated to true singular values,
    $\sigma_i / \sigma_{i}^{(\text{true})}$, for rSVD on $K$
    (Algorithm~\ref{alg:discrete_rsvd}) and on $K^*$
    (Algorithm~\ref{alg:discrete_rsvd_Kstar}) across different oversampling
    parameters $p$ (mean $\pm 1$ standard deviation over 20 repetitions using Problem I.)}
    \label{fig:approximation_of_singular_values}
\end{figure*}
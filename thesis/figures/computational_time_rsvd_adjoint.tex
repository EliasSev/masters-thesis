\begin{figure*}[thbp]
    \centerline{\includegraphics[width=1.0\textwidth]{computational_time_of_adjoint_rsvd.png}}
    \caption{The relative computational time $t^* / t$ where $t^*$ is the time using rSVD on $K^*$ (Algorithm~\ref{alg:discrete_rsvd_Kstar}) and $t$ using rSVD on $K$ (Algorithm~\ref{alg:discrete_rsvd}) for selected $k$ and increasing $N$. For rSVD on $K^*$, both time with and without recovery of the SVD is measured.}
    \label{fig:computational_time_rsvd_adjoint}
\end{figure*}